# Datos crudos SNIFA

Los archivos trimestrales originales `PH*.csv` no se versionan porque ocupan aproximadamente 3 GB.

Para reconstruir la auditorÃ­a, descargue los archivos de datos abiertos de centrales termoelÃ©ctricas de SNIFA y guÃ¡rdelos en esta carpeta conservando sus nombres originales.

El repositorio sÃ­ incluye las dos bases reducidas necesarias para revisar y reproducir el modelamiento:

- `prototipo_entrega/data/angamos1_nox_horaria_filtrada.csv`;
- `prototipo_entrega/data/angamos1_nox_semanal_modelo.csv`.
